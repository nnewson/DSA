# BloomFilter in Rust

Run a basic sample client

```bash
cargo run
```

Run a test suite

```bash
cargo test
```

You can also build and test in a Docker container.

Build the container:

```bash
docker build -t bloom-filter-rust-test .
```

Run the tests in the container:

```bash
docker run --rm bloom-filter-rust-test
```
